// =============================================================
// Runtime Supabase config override.
// Edit this file AFTER deploying to point the app at YOUR own
// Supabase project (Firebase Hosting, Cloud Run, anywhere).
//
// Leave the values empty ("") to keep using the default Lovable
// Cloud backend that ships with the build.
// =============================================================
window.__APP_CONFIG__ = {
  SUPABASE_URL: "",          // e.g. "https://xxxx.supabase.co"
  SUPABASE_ANON_KEY: "",     // your own anon/publishable key
};
